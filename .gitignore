# Original material, kept locally and never committed. Hundreds of megabytes of
# hi-res photography, the roster spreadsheet, and the two reference wikis we
# studied, which are other teams' code.
_source/

# Rebuildable output. docs/index.html is committed because Pages serves it;
# these two are the same bundle and would only double the churn.
dist/

.DS_Store
